<div>
  <%= month_calendar events: @events do |date, events| %>
    <%= date.day %>
    
    <%events.each do |event| %>
      <div>
        <h4> <%=event.name%></h4>
        <%=event.location%>
      </div>
    <% end %>
  <% end %>
</div>

<!-- add events in the right sidebar-->